/**
 * The list of available commands.
 */
export namespace Commands {
    /**
     * The `randNumber` command.
     * Generates a random number.
     */
    export const RANDOM_NUMBER = 'randomizer.randNumber';
    /**
     * The `randNumberInBetween` command.
     * Generates a random number in between two numbers provided by users.
     */
    export const RANDOM_NUMBER_IN_BETWEEN = 'randomizer.randNumberInBetween';
}
